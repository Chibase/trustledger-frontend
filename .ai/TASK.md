# Current TrustLedger Task

Status: EMPTY

No implementation task is currently assigned.

**EMPTY TASK = do not implement product changes.** Do not infer an active packet from `docs/BUILD_PLAN.md`. A non-empty TASK is the only authorised current implementation assignment.

Before beginning any substantive implementation, the assigned AI implementation agent must:

1. Read `AGENTS.md`.
2. Read `.ai/TASK.md` and `.ai/HANDOFF.md`.
3. Read `docs/BUILD_PLAN.md`.
4. Read `docs/DECISIONS.md`.
5. Read `docs/DESIGN_SYSTEM.md`.
6. Inspect the relevant existing implementation.
7. Identify the smallest compatible change.

When this file contains an active task, execute only that task and its explicitly required supporting changes. Obey `AGENTS.md`, `docs/BUILD_PLAN.md`, `docs/DECISIONS.md`, and `docs/DESIGN_SYSTEM.md`.
