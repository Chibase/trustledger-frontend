# TrustLedger SRM — Demo: Consolidated Evidence

Offline VIP-style demo pack for a private plan that consolidates scattered field tools into an evidence-backed, ledgered SRM.

**Package:** [`docs/exports/srm-demo/`](exports/srm-demo/README.md)  
**Zip:** `docs/exports/trustledger-srm-demo.zip` (generated; synthetic data only)

This is **not** the consortium 30-minute showcase (`DEMO_PLAN_SRM_SHOWCASE.md`). It is a 12–15 minute **consolidated evidence** demo with organisations, sites, assets, inspections, incidents, work orders, 400 evidence rows, and 500 chained ledger entries.

Live Cloud creation still uses the VIP Owner path (TL Project / Incident / Evidence). Full registers ship as CSV until those DocTypes exist on Cloud.

Integrity: illustrative Eastern Cape-like coordinates; mock `@example.local` contacts; never present as a customer matter.
