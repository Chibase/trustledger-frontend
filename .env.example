NEXT_PUBLIC_DEV_ROLE=client
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# demo (default) | live — live calls Interserv Frappe, falls back to mock on error
NEXT_PUBLIC_DATA_MODE=demo
# Keep true until srm-core AI endpoints exist. Set false to call Frappe AI methods.
NEXT_PUBLIC_AI_MOCK=true
